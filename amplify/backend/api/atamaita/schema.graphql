type HeadacheReport @model @auth(rules: [{allow: owner}]) {
  id: ID!
  onsetDate: String!
  onsetTime: String!
  impact: String!
  duration: Int
  memo:String
  prodrome: Boolean
}
type UserSettings @model @auth(rules: [{allow: owner}]) {
  id: ID!
  defaultImpact: Int
  template: String
}
