type HeadacheReport 
@model 
@auth(rules: [{allow: owner}]) 
@key(
  name:"ByOwner"
  fields: ["owner","onsetDate"],
  queryField: "headacheReportByOwner"
  ) {
  id: ID!
  onsetDate: String!
  onsetTime: String!
  curedDate: String
  curedTime: String
  impact: Int!
  memo:String
  prodrome: Boolean
  owner: String

}
type UserSetting @model @auth(rules: [{allow: owner}]) {
  id: ID!
  defaultImpact: Int
  template: String
}
